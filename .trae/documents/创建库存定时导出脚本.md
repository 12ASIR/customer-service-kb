我将为您创建一个 Node.js 脚本，用于定时导出库存查询数据到 Excel 文件。

该脚本将包含以下功能：
1.  **自动分页拉取**：自动处理 `pageNum`，循环请求直到获取所有库存数据。
2.  **Excel 导出**：使用项目中已有的 `xlsx` 库将数据保存为 `.xlsx` 文件。
3.  **定时执行**：内置简单的定时器（默认每小时执行一次，可配置），也支持单次运行。
4.  **签名处理**：目前默认配置使用 `openApiAdminSecret` (测试环境跳过签名)。我会在代码中预留签名函数的接口，如果生产环境需要，您可以填入具体的签名算法。

**主要步骤：**
1.  在 `app_722906138626` 目录下创建 `scripts` 文件夹。
2.  创建 `scripts/export-inventory.js` 脚本文件。
3.  **注意**：脚本中 `API_BASE_URL` 需要您在运行前手动修改为实际的 API 域名（因为文档中未提供）。

**依赖说明：**
- 使用 Node.js 原生 `fetch` (Node 18+) 发送请求，无需安装 `axios`。
- 使用项目中已有的 `xlsx` 库。

准备好后，您可以直接使用 `node scripts/export-inventory.js` 运行。