# Supabase 配置 (用于多人协作共享数据)
# 1. 访问 https://supabase.com/ 注册账号并创建一个新项目 (Project)
# 2. 在项目设置 -> API 中找到 URL 和 anon key
# 3. 将它们填入下方
# 4. 在 Supabase 的 SQL Editor 中运行以下建表语句：
# 
# create table kb_items (
#   id text primary key,
#   sku text,
#   category text,
#   vehicle_model text,
#   problem_level text,
#   problem_type text,
#   problem_description text,
#   standard_answer text,
#   internal_solution text,
#   error_avoidance text,
#   update_time text,
#   attachments text,
#   is_deleted boolean default false
# );
# 
# 5. 重启本地开发服务 (npm run dev)

VITE_SUPABASE_URL=https://oklzbqabzhdinsuqfead.supabase.co
VITE_SUPABASE_KEY=sb_publishable_6_uCkyLeTs-JLkspC1bawA_CieoCwZ3